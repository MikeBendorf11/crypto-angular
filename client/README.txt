To build this project, first use the command prompt or terminal navigate into the folder which contains the package.json file. Then run the command:

	npm install

Then run the command to build and run the application in the browser:
	ng serve